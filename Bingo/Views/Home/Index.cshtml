@model List<int>
@{
    ViewData["Title"] = "Home Page";
}

<div class="container text-center">
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-4">
        <div class="col celdaCarton">Column</div>
        <div class="col">Column</div>
        <div class="col">Column</div>
        <div class="col">Column</div>
    </div>
    @foreach (var item in Model)
    {
        <div>@item</div>
    }
</div>