@using Bingo.ViewModels;
@model CartonViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="row">
    <div class="col-sm-6">
        <h5 id="labelCarton1"></h5>
        <div class="text-center carton1">
           @foreach (var item in Model.Carton1)
           {
               @if (item == 0)
               {
                   <div class="celdaVaciaCarton1"></div>
               }
               else
               {
                   <div class="celdaCarton1">@item</div>
               }
           }
         </div>
    </div>

    <div class="col-sm-6">
        <h5 id="labelCarton2"></h5>
        <div class="text-center carton2">
           @foreach (var item in Model.Carton2)
           {
           @if (item == 0)
            {
                <div class="celdaVaciaCarton2"></div>
            }
            else
            {
                <div class="celdaCarton2">@item</div>
            }
        }
        </div>
    </div>
</div>
<div class="row">
    <div class="col-sm-6">
        <h5 id="labelCarton3"></h5>
        <div class="text-center carton3">
            @foreach (var item in Model.Carton3)
            {
                @if (item == 0)
                {
                    <label class="celdaVaciaCarton3"></label>
                }
                else
                {
                    <label class="celdaCarton3">@item</label>
                }
            }
        </div>
    </div>

    <div class="col-sm-6">
        <h5 id="labelCarton4"></h5>
        <div class="text-center carton4">
            @foreach (var item in Model.Carton4)
            {
                @if (item == 0)
                {
                    <label class="celdaVaciaCarton4"></label>
                }
                else
                {
                    <label class="celdaCarton4">@item</label>
                }
            }
        </div>
    </div>
</div>

<br/>
<div class="panelSorteo">
    <button class="btn btn-primary" onclick="LanzarBolilla()" id="btnLanzarBolilla">Lanzar bolilla</button>
    <br />
    <div class="labelBolillaSection">
        <h4 class="panel">Bolilla: </h4>
        <h4 id="labelBolillero" class="panel"></h4>
    </div>
    <div class="labelCartonesGanadoresSection">
        <h4 class="panel">Cartones ganadores: </h4>
        <h4 id="cartonesGanadores" class="panel"></h4>
    </div>
</div>


@section Scripts{
    <script type="text/javascript" src="~/js/site.js"></script>
}
   